<UserControl x:Class="Prism8WpfDialogRegionSample.Views.ContentView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Prism8WpfDialogRegionSample.Views"
        xmlns:prism="http://prismlibrary.com/"
        mc:Ignorable="d"
>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Command="{Binding DialogACommand}">DialogA</Button>
        <Button Grid.Column="1" Command="{Binding DialogBCommand}">DialogB</Button>
    </Grid>
</UserControl>
