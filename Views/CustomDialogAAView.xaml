<UserControl x:Class="Prism8WpfSample.Views.CustomDialogAAView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Prism8WpfSample.Views"
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="200">

    <StackPanel>
        <Button Command="{Binding CustomDialogABCommand}">to CustomDialogAB</Button>
        <Button Command="{Binding CustomDialogBACommand}">to CustomDialogBA</Button>
        <Button Command="{Binding ContentViewCommand}">to ContentView</Button>
    </StackPanel>
</UserControl>